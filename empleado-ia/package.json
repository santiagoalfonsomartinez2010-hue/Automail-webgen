{
  "name": "empleado-ia",
  "version": "1.0.0",
  "description": "Empleado IA de Atención al Cliente",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
